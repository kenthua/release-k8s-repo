apiVersion: configmanagement.gke.io/v1
kind: ConfigManagement
metadata:
  name: config-management
spec:
  clusterName: release-k8s
  enableMultiRepo: true
  policyController:
    enabled: true
    logDeniesEnabled: true